import styled from 'styled-components';

const SubInfoStyle = styled.span`
  color: hsl(var(--white));
  font-size: var(--small_font);
  font-weight: var(--font_w_light);
`;

export default SubInfoStyle;
