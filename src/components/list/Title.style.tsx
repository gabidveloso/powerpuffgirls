// Import Packages
import styled from 'styled-components';

const TitleStyle = styled.h2`
  color: hsl(var(--white));
  font-size: var(--sub_header_font);
  font-weight: var(--font_w_regular);
  margin: 0;
`;

export default TitleStyle;
