// Import Packages
import styled from 'styled-components';

const SeparatorBallStyle = styled.div`
  background-color: hsl(var(--white));
  border-radius: 50%;
  height: 0.5rem;
  width: 0.5rem;
`;

export default SeparatorBallStyle;
